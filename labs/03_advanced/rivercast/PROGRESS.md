# RiverCast — Phase Progress

Status values: `not started` · `in progress` · `in review` · `blocked` · `done`.

An agent sets a phase to `in review` when it hands off completed work; the maintainer commits, pushes, opens the PR, links it here, and sets `done` after merging. See `CLAUDE.md` rule 21.

| # | Phase | Status | PR | Notes |
|---|---|---|---|---|
| 0 | Record scope and architecture decisions | in review | [open from `rivercast` branch](https://github.com/MohammadB88/MLOps-Workshop-Exercises/pull/new/rivercast) | ADRs 0001–0003, configs/base.yaml, README educational label |
| 1 | Create the JupyterLab workbench and project skeleton | in review | | Package, typed config, JSON logging, local object store, CLI, envcheck notebook, workbench docs + bootstrap. Maintainer commits/opens PR |
| 2 | Run a data-viability spike | in review | | Gate passed: PROCEED. Spike run live 2026-08-02; UUIDs pinned, fixtures committed, report in docs/data_viability_report.md. Maintainer commits/opens PR |
| 3 | Implement source adapters and immutable raw storage | in review | | PegelOnlineSource + FixtureGaugeSource behind one protocol, shared parser, bronze RawArchive (immutable, checksum-idempotent), ingest flow, contract tests. Maintainer commits/opens PR |
| 4 | Build canonicalization and data-quality contracts | in review | | Silver contracts, normalize (dedup + documented conflict rule), hourly resample (explicit missingness, no interpolation), fail-closed quality checks, DST regression tests on real 2025 fixtures. Maintainer commits/opens PR |
| 5 | Build leakage-safe features and labels | in review | | Lag/rolling/calendar/missingness features, 6h/12h labels, content-hashed dataset manifest, mandatory leakage tests (mutation, label-in-features, centered-window, lag-future checks) all passing. Added pandas/pyarrow/numpy deps. Maintainer commits/opens PR |
| 6 | Establish baselines and offline evaluation | in review | | Temporal split, persistence/ridge/HistGradientBoosting, MAE/RMSE/skill + slice metrics, serialization parity, `rivercast train` CLI. Ridge beats persistence at both horizons on the fixture dataset; HGB honestly reported as not beating it (overfits on 168 rows) — see reports/baseline/baseline_report.md. Added scikit-learn dep. Maintainer commits/opens PR |
| 7 | Add MLflow tracking and registry | in review | | Full run logging (params/metrics/slices/manifest/signature/tags) via `rivercast.models.tracking`; registry + challenger/champion promotion transaction via `rivercast.models.registry` (bootstrap, idempotent registration, real champion-metrics comparison, deploy-failure never moves champion). `rivercast train --track-mlflow`/`--promote` CLI flags. Downgraded pandas 3.0.5 → 2.3.3 (mlflow's own metadata caps pandas <3 on every current release); re-ran full Phase 4-6 suite with no regressions. Local sqlite tracking-URI default added so fixture mode needs no live server. Maintainer commits/opens PR |
| 8 | Containerize reusable pipeline components | in review | | 10 components (`fetch`, `transform`, `validate`, `train`, `evaluate`, `register`, `promote`, `forecast`, `monitor`, `deploy`), each a plain `run()` function + thin CLI, reading/writing via object-store keys, small JSON result envelope, fail-closed. `tests/integration/test_components_end_to_end.py` runs the full chain against real (isolated) object storage + MLflow. Found and fixed two real upstream/pre-existing bugs: an MLflow-on-Windows `models:/` URI resolution bug (worked around via `runs:/<run_id>/model`, see `docs/pipeline_components.md`) and a Phase 7 gap where a fresh sqlite-backed MLflow experiment defaulted its artifact store to the process CWD instead of the isolated storage root. Four Containerfiles (`rivercast-data/train/ops/serving`), non-root (UID 1001); `pip install .` + each entrypoint verified against a clean staged copy (no Docker daemon available here, per CLAUDE.md rule 19). KFP compatibility verified at the component-signature and compile level; full `SubprocessRunner` execution not verifiable on this Windows dev box (documented, re-verify in the Linux workbench at Phase 9). Added `kfp` dev dependency. Maintainer commits/opens PR |
| 9 | Implement `rivercast-data-ops` pipeline | in review | | `pipelines/data_ops_pipeline.py` compiles to `pipelines/compiled/rivercast-data-ops.yaml`: fetch (ParallelFor per station) → transform → validate (now includes a real target-station freshness gate) → `dsl.If(validate.output == "ok")` → forecast (ParallelFor per horizon, persists a `PredictionRecord`), plus monitor and a new join-matured-predictions/delayed-metrics step (`rivercast.processing.delayed_metrics`), both running independently of the gate. Added `contracts/predictions.py` (`PredictionRecord`, `MaturedPrediction`) — the Phase 8 `forecast` component now actually persists predictions to the `predictions` zone. Caching disabled on fetch/validate/forecast/delayed-metrics per the plan's schedule; `transform` keeps default caching. Found and fixed two real KFP incompatibilities: `@dsl.component` cannot coexist with `from __future__ import annotations` (misreads every type annotation as a literal string), and pipeline-parameter-typed lists (`list[int]`) are `PipelineParameterChannel`s at compile time requiring `dsl.ParallelFor`, not a bare `for` loop — both documented in `docs/pipeline_components.md`. `notebooks/05_pipeline_development.ipynb` demonstrates every step directly (including the freshness gate's pass/fail behavior) and compiles the YAML; full KFP `SubprocessRunner` execution reproduces the same Windows/shell incompatibility documented in Phase 8, re-verify in the Linux workbench. `tests/integration/test_data_ops_pipeline.py` covers all 4 non-infrastructure Phase 9 acceptance criteria (compiles to YAML; idempotent fetch; stale-source blocks forecasting; matured predictions receive actual values/errors) — "pipeline artifacts/logs visible in OpenShift AI" and "hourly recurring run creatable/removable" need a real cluster, out of scope here. Maintainer commits/opens PR |
| 10 | Implement `rivercast-model` pipeline | in review | | `pipelines/model_pipeline.py` compiles to `pipelines/compiled/rivercast-model.yaml`: trigger → `dsl.If(should_train == True)` → train → evaluate → register → promote (register→challenger→**real** deploy/smoke-test→champion transaction; a failed or omitted smoke test never moves champion, rule 14). Added `components/trigger` (check-training-trigger: skips when fewer than `thresholds.retraining.min_new_labeled_rows` labeled rows exist for the horizon, or an MLflow run already exists for the same full `dataset_id`+horizon — found and fixed a real bug where an early draft compared a truncated 12-char dataset id against the full hash logged in run params and always found zero matches). Extended `components/promote` to call the real `components.deploy` in-process as its smoke-test callable instead of an always-pass stub (required updating one Phase 8 test that asserted auto-promotion without a smoke test). `tests/integration/test_model_pipeline.py` covers all 4 Phase 10 acceptance scenarios directly against real fixture data: (1) the real fixture window's 161/155 trainable rows are naturally under the default 168 threshold, exercising "no new data skips" for free; (2) hist-gradient-boosting at 12h is genuinely rejected (Phase 6 finding); (3) a deliberately-broken smoke-test payload leaves an existing champion unchanged; (4) a full real promotion moves the champion. `tests/unit/test_component_trigger.py` covers the trigger component standalone. Maintainer commits/opens PR |
| 11 | Build the serving layer | in review | | FastAPI service (`/health`, `/ready`, `/metadata`, `/predict`) in `src/rivercast/serving/`; `Predictor` loads each horizon's champion straight from the MLflow registry and validates/casts requests against the model's own signature rather than a hand-written schema (design rule: no independent feature-contract reimplementation). KServe `InferenceService`/`ServingRuntime` base manifests plus local/openshift Kustomize overlays under `deploy/`. `Containerfile.serving` now installs and runs the app; `pyproject.toml` gained `fastapi`/`uvicorn` (runtime) and `httpx` (dev/test) deps. `tests/smoke/test_serving.py` drives the real train→register→promote→serve chain in-process via `TestClient` (no Docker/cluster here per rule 19) and covers all Phase 11 acceptance scenarios reachable without one: metadata reporting, 4xx on bad horizon/missing features, two horizons loaded independently with no cross-talk, and rollback-by-re-promotion restoring prior predictions. Found and fixed two real bugs while verifying: the smoke test's `_materialize_gold_dataset` helper was passing `tmp_path` instead of the real `lab_root` fixture as the fixture-data directory, and MLflow's strict schema enforcement rejected `missing_<station>` flag columns because a JSON-decoded request is always `float64` while the model's signature declares them `int64` (fixed by casting the request row to the champion's own signature dtypes before scoring, in `Predictor.predict`). Also fixed a real mypy error (`ModelVersion.run_id` is `str \| None`; now fails closed with `PredictorError` if a champion has no run_id, per rule 10) and three ruff findings. `make lint`, `make typecheck`, and the full `pytest` suite (all phases) pass. Not verified here: real KServe `InferenceService` readiness/liveness wiring and a live Docker smoke test (both need cluster/Docker infra unavailable in this environment). Maintainer commits/opens PR |
| 12 | Implement delayed monitoring and retraining signals | in review | | New `src/rivercast/monitoring/` package: `data_quality.py` (existing Phase 4 checks + Evidently `DataSummaryPreset`), `drift.py` (Evidently `DataDriftPreset` over a reference/current gold-dataset split; fails closed on empty input), `performance.py` (rolling delayed MAE/RMSE + error-vs-persistence + rising/falling/level-quantile slices via a real Evidently `RegressionPreset`, plus `evaluate_retraining_signal` implementing the plan's retraining-decision-artifact schema exactly). Retraining fires on genuine performance degradation only — `rolling_mae >= persistence_mae * thresholds.monitoring.performance_degradation_mae_ratio` — and is withheld below `min_matured_predictions_for_signal` even if the ratio would trip; drift is reported separately and never feeds the decision (ADR 0003). Extended `components/monitor` to compute all three, per configured horizon, best-effort (fails plainly to `null` sections rather than raising when no champion/predictions/dataset exist yet — satisfies "reports work with no labels and with delayed labels"). Added `thresholds.monitoring` to `config.py`/`configs/base.yaml`. Wired `dataset_short_id` through `monitor_task` in `pipelines/data_ops_pipeline.py` so drift reports actually activate in the compiled pipeline. Discovered Evidently 0.7.21 (latest) removed the legacy `evidently.report.Report`/`metric_preset` API the plan's reference docs describe; verified the current `evidently.Report`/`evidently.presets`/`evidently.Dataset` API directly against real data before writing any production code (documented in `docs/operations.md`). Renamed `models.evaluate._mae`/`_rmse` to public `mae_cm`/`rmse_cm` so `monitoring.performance` could reuse the same slicing helpers instead of duplicating them. Fixed two stale docstring references (`rivercast.monitoring.delayed...` pointing at a module that was never created — the real function lives in `rivercast.processing.delayed_metrics`). `tests/unit/test_monitoring_{drift,performance,data_quality}.py` cover the plan's specific fixtures (synthetic drift warning, performance-degradation retraining request, too-few-matured-predictions withholding the signal); `tests/integration/test_monitor_delayed_signals.py` exercises the same logic through `components.monitor` against a real trained/registered/promoted champion and a real matured forecast, including "a monitor run reporting drift never itself moves champion." Added `evidently==0.7.21` dependency and a `evidently.*` mypy override. `make lint`, `make typecheck`, and the full `pytest` suite (all phases) pass. Not verified here: Evidently HTML reports rendered/inspected in a real OpenShift AI workbench; a genuine multi-week rolling window against live PEGELONLINE data. Maintainer commits/opens PR |
| 13 | Add CI and release automation | not started | | |
| 14 | Integrate with the workshop learning flow | not started | | |
| 15 | Harden before calling it complete | not started | | |
| A | Extension — Add DWD observations | not started | | Only after the gauge-only loop works |
| B | Extension — Add DWD ICON-D2 forecasts | not started | | Advanced data-engineering exercise |
| C | Extension — Multi-station models | not started | | |
| D | Extension — Canary rollout | not started | | After direct replacement works reliably |

## Current phase

Phases 0–12 — in review; maintainer commits and opens PRs (one phase per PR). Phase 2's gate decision was PROCEED (docs/data_viability_report.md). Next: Phase 13 (CI and release automation).

## Blockers

None recorded.
